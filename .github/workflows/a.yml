on:
  push:
    paths:
      - "a_folder/**"

name: "a"

jobs:
  write_message:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - run: |
          echo "A ran!!" >> message.txt
      - uses: actions/upload-artifact@v2
        with:
          name: message
          path: message.txt